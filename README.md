# CalculatorApi
.NET 8 REST API accepting two numbers and performing a dynamically specified arithmetic operation via HTTP Header, secured with JWT, defined by YAML (SwaggerHub), and containerized with Docker, including unit tests and documentation.
